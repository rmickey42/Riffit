<main>
    <h1>{{user.username}}'s Dislikes</h1>
    <ul>
        {{#each user.dislikedPosts}}
        <li>
            {{> post post=this}}
        </li>
        {{/each}}
    </ul>
    <a href="/">Return to the homepage</a>
</main>