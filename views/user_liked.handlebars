<main>
    <h1>{{user.username}}'s Likes</h1>
    <ul>
        {{#each user.likedPosts}}
        <li>
            <a href="/posts/{{this._id}}">View Post</a>
        </li>
        {{/each}}
    </ul>
    <a href="/">Return to the homepage</a>
</main>